#include <bits/stdc++.h>
using namespace std;


int main()
{
    int a;
    cin>>a;
    // for ignore enter for not gate any travel
    cin.ignore();
    // if ignore not working then 
    // getchar();
    string s;
    getline(cin,s);
    cout<<a<<endl;
    cout<<s<<endl;
    return 0;
}